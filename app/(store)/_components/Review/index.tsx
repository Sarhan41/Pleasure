import { Review } from "@/types";

interface ReviewProps {
  reviews: Review[];
}

const ProductReviews = ({ reviews }: { reviews: ReviewProps }) => {
  return <div></div>;
};

export default ProductReviews;
